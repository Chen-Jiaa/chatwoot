{
  "build": {
    "dockerfilePath": "docker/Dockerfile",
    "context": ".",
    "buildArgs": {
      "GIT_SHA": "env:GIT_COMMIT_SHA"
    }
  },
  "deploy": {
    "startCommand": "bundle exec rails db:migrate && bundle exec rails server -p $PORT"
  }
}
